[package]
name = "ard-math"
version.workspace = true
edition.workspace = true

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
glam = { version = "0.24", features = ["serde", "bytemuck", "mint"] }